<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Goshen Youth Ministry</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        .container {
            width: 80%;
            margin: 0 auto;
            padding: 20px;
        }
        h1 {
            text-align: center;
            color: #333;
        }
        .section {
            margin-bottom: 30px;
        }
        .section h2 {
            color: #555;
            border-bottom: 2px solid #ddd;
            padding-bottom: 10px;
        }
        .item {
            background-color: #fff;
            padding: 15px;
            margin-bottom: 10px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }
        .item h3 {
            margin: 0;
            color: #333;
        }
        .item p {
            margin: 5px 0;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Goshen Youth Ministry</h1>

        <!-- Financial Updates Section -->
        <div class="section">
            <h2>Financial Updates</h2>
            {% for update in financial_updates %}
                <div class="item">
                    <h3>{{ update.title }}</h3>
                    <p><strong>Amount:</strong> ${{ update.amount }}</p>
                    <p>{{ update.description }}</p>
                    <p><em>Date:</em> {{ update.date }}</p>
                </div>
            {% endfor %}
        </div>

        <!-- Notifications Section -->
        <div class="section">
            <h2>Notifications</h2>
            {% for notification in notifications %}
                <div class="item">
                    <h3>{{ notification.title }}</h3>
                    <p>{{ notification.message }}</p>
                    <p><em>Date:</em> {{ notification.date }}</p>
                </div>
            {% endfor %}
        </div>
    </div>
</body>
</html>